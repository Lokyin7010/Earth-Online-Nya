# 管理员校准规则

## 定义

`管理员校准` 是 demo 的正式机制。

它表示：

- 管理员基于真实观察
- 或出于演示需要
- 对测试用户当前五维进行直接修正

这不是隐藏逻辑，而是要明确显示在系统里的行为。

## 适用场景

### 1. 用户主观体感与系统计算不一致

例如：

- 系统判断 `FOCUS = 62`
- 但管理员认为该用户此刻思路明显更清晰
- 于是执行 `FOCUS +5`

### 2. Demo 演示需要快速构造状态

例如：

- 演示“疲劳边缘”
- 演示“恢复期”
- 演示“状态极佳”

### 3. 修正异常录入

例如：

- 用户选错了事件
- 某天日志缺失
- 测试中需要人工回拨数值

## 允许修改的字段

- `hp`
- `ep`
- `mood`
- `stress`
- `focus`
- `hpMax`（仅管理员）

## 修改方式

建议界面提供两种方式：

### 方式 A：增量修改

管理员点按钮：

- `-10`
- `-5`
- `-1`
- `+1`
- `+5`
- `+10`

优点：

- 快
- 适合 demo
- 容易形成操作手感

### 方式 B：直接设值

管理员直接输入最终值，例如：

- `FOCUS = 78`

优点：

- 适合快速构造精确状态

建议第一版两个都保留。

## 必填日志字段

每次校准必须写入：

- `id`
- `createdAt`
- `adminUserId`
- `targetUserId`
- `dimension`
- `before`
- `after`
- `delta`
- `reason`
- `type = admin-calibration`

## reason 建议选项

为了避免每次都手打长备注，建议先做预设原因，再允许补充文本。

预设原因：

- `主观状态修正`
- `演示用状态构造`
- `误操作回滚`
- `补录缺失数据`
- `测试校准`

补充文本示例：

- `用户反馈今天精神比系统判断更好`
- `为演示恢复期，临时提升 HP 与 MOOD`
- `刚才误选了长时间盯屏，现回滚`

## 前台展示方式

管理员校准记录应该出现在日志里，但显示层级低于每日事件结算。

建议展示为：

- `管理员校准：专注值 +5`
- `管理员校准：压力值 -8`
- `管理员校准：健康值重设为 82`

如果有原因，则追加：

- `原因：主观状态修正`

## 与系统结算的关系

建议公式：

```text
当前显示值 = 基础值 + 今日事件结算 + 联动修正 + 管理员校准
```

这样好处是：

- 事件系统仍然成立
- 管理员校准可追踪
- 后续要区分“系统判断”和“人工修正”很容易

## 权限边界

### 管理员可以

- 查看所有测试用户
- 修改任意测试用户五维
- 修改 `hpMax`
- 查看全部校准日志

### 测试用户不可以

- 修改自己的最终五维
- 修改其他用户
- 删除管理员校准日志
- 创建新账号

## 第一版简化建议

第一版先不做复杂审核流。

只做：

- 管理员修改
- 自动写入日志
- 前台可查看记录

这已经足够支撑 demo。
